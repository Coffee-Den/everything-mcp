{
  "name": "everything-mcp",
  "version": "1.0.0",
  "description": "MCP server for Everything file search (voidtools)",
  "main": "dist/index.js",
  "type": "module",
  "bin": {
    "everything-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
